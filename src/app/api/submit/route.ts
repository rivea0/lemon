// import insertInto from '@/app/db/addData'

// export async function POST(request: Request) {
//   const formData = await request.formData()
//   const title = formData.get('title')
//   const id_color = formData.get('id_color')
//   const notes = formData.get('notes')
//   const imageFilePath = formData.get('imageFilePath')

//   insertInto({ title, id_color, notes, imageFilePath })
//   return Response.json({ title, id_color, notes, imageFilePath  })
// }
